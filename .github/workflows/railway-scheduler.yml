name: Keep Railway App Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # chạy mỗi 5 phút
  workflow_dispatch:       # cho phép chạy thủ công từ tab Actions

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Gửi ping để giữ server Railway hoạt động
        run: |
          echo "🔄 Đang ping ứng dụng Railway..."
          curl -I https://phonghocnhom-production.up.railway.app || echo "Không thể ping được ứng dụng!"

      - name: In thông tin dự án
        run: |
          echo "📦 Project ID: 3826dccf-6cf2-4755-8d34-4fe7a957e0c3"
          echo "🖥️  Service ID: d3362d88-1c5b-4e7f-a0f7-cd5dc90c5ce3"
